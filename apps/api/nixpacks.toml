[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install", "pip install markitdown[all]"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
